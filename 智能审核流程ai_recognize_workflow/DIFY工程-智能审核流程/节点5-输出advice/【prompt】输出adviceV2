# 任务
根据智能审核结果，生成一段面向审核人员展示的简洁审核结论。

# 输入
总审核结果：{{#17680435327510.finalResult#}}
智能审核结果：{{#1769426125740.output#}}

# 输出目标
让审核人员快速看懂：
- 本次审核是否通过
- 若未通过，核心未通过点是什么
- 哪些规则要求未满足

# 总结范围
1. 必须先读取“总审核结果：{{#17680435327510.finalResult#}}”，它是唯一的总审核结论。
2. 若总审核结果为“通过”，固定输出“本次审核通过。已满足认定规则组合要求。”，不要再总结 `ruleResults` 中的单条不通过规则。
3. 若总审核结果为“不通过”，再根据 `ruleResults` 中 `ruleResult` 为“不通过”的规则进行总结，重点参考 `ruleContent`、`suspicionList.detail`、`suspicionList.sources`。
4. 不要根据 `ruleResults` 中是否存在单条“不通过”来推断总结果；总结果只能以“总审核结果”为准。
5. 如果“总审核结果”为空或不是“通过/不通过”，不要自行判断通过或不通过，应输出“未获取到总审核结果，无法生成审核结论。”
6. 总结不通过原因时，不要机械罗列所有单条不通过规则。若某条不通过规则属于未命中的备选分支或互斥分支（如一型/二型、不同诊断路径、不同治疗路径），且已有同类备选规则通过，不要将该备选分支的不通过作为核心未通过原因。
7. 若总审核结果为“不通过”，但可总结的不通过规则仅属于未命中的备选分支，应输出“本次审核未通过。现有规则组合未满足完整认定条件，请核查必要规则是否缺失或未通过。”
8. 不要复述 `reasoningContent`。
9. 总结不通过原因时，仅总结真正导致总结果不通过的规则，忽略通过规则和未命中的备选分支规则。

# 表达要求
1. 输出一段完整中文，不分点、不换行、不加标题
2. 语言简洁、客观、自然，符合审核结论口吻
3. 优先概括“规则要求 + 未满足原因”
4. 多条不通过时，合并同类问题，避免逐条机械重复
5. 如有明确指标或关键事实，可保留最关键的1到2项
6. 避免使用“存在一条不通过规则”“根据推理”“分析可见”等生硬表述
7. 不要输出思考过程，不要输出Markdown，不要输出JSON，不要输出`<think>`

# 输出长度
控制在 50-90 字，必要时可略超，但应尽量精炼。

# 输出风格示例
- 本次审核未通过。现有材料虽支持xxxx，但xxxxxx，且未提供xxxxxx的有效证据。
- 本次审核未通过。材料明确记载XXXXX，现有证据不支持“XXXXXX”这一认定要求。
- 本次审核通过。已满足认定规则组合要求。

# 现在开始
请直接输出最终结论文本：
